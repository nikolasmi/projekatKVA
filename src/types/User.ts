export interface User {
    userId: number | null,
    name: string | null,
    surname: string | null,
    email: string | null,
    phone: string | null,
    address: string | null,
    username: string | null,
    password: string | null,
    favouriteItems: number | null
}